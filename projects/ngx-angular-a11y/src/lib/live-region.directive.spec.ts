import { LiveRegionDirective } from './live-region.directive';

describe('LiveRegionDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveRegionDirective();
    expect(directive).toBeTruthy();
  });
});

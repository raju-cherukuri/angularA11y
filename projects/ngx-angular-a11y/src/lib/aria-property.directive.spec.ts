import { AriaPropertyDirective } from './aria-property.directive';

describe('AriaPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaPropertyDirective();
    expect(directive).toBeTruthy();
  });
});

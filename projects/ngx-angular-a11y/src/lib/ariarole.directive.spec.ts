import { AriaroleDirective } from './ariarole.directive';

describe('AriaroleDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaroleDirective();
    expect(directive).toBeTruthy();
  });
});
